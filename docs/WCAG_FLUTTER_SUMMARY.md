# WCAG 2.2 AA & Flutter — Ringkas 2026-08-30

- **Web**: landmark `<header>/<nav>/<main>`, `h1→h2` hierarki, `label`+`aria-describedby` tiap input, `alt` bermakna, keyboard tab order, focus visible `outline:2px`, skip-link `#main`, modal `focus trap` + `aria-modal`, contrast ≥4.5:1, target ≥44×44, zoom 400% reflow, error summary `role=alert` link ke field, `aria-live` untuk status, loading/skeleton/empty/error/retry/success/confirm/unsaved/duplicate/offline states konsisten.
- **Form laporan**: langkah 3 section + autosave draf `localStorage` 30s + indikator sync, validasi progresif, geolocation fallback manual, kompresi foto `ImageCompressor` 2 MB, preview sebelum submit, label `Dikirim` hanya UI untuk `Submitted`.
- **Flutter**: `flutter analyze` 0 issue (target), `BuildContext` async gap diperbaiki `if (!mounted) return`, deprecated `WillPopScope`→`PopScope`, nullability, widget terpecah `ReportFormWidget` testable, `cleartextTraffic=false`, `secure_storage`, `network_security_config.xml`, permission kamera/lokasi kontekstual, token redaction, SQLite `sqflite` migration, conflict `updated_at` wins, `flutter test` + `build apk --release` fail bila `keystore` missing (gradle `signingConfig` required, no debug fallback), `sqlite3.dll` via `sqlite3_flutter_libs`.
